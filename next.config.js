/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'i.blogs.es', 'www.3djuegos.com'],
  },
};

module.exports = nextConfig;
